# v4.0.4 — 快捷命令、服务诊断、原生体验

## 🚀 新功能
- **快捷命令**：项目行 ▶ 按钮展开命令菜单——命令完全自定义（名称 + shell 命令 + 3 个预设图标 play/rocket/wrench），运行/添加/删除一步到位，无预设命令
- **后端服务诊断**：左上角 logo 右侧状态点（绿/黄/红）→ 点击展开面板：omp 运行时、网络代理、RPC 会话、服务器信息，支持一键重启 OMP 会话
- **更新说明升级**：更新弹窗直接显示本次版本的 Release 说明（不再只是跳转链接）；设置 → 更新历史可点击查看任意历史版本说明
- **原生文件夹选择**：选择工作区时调用系统文件夹对话框（macOS/Windows/Linux 三端），可在弹窗内切换回网页版浏览
- **开屏动画设置**：设置 → 开屏动画 —— 每次启动播放 / 仅首次 / 关闭
- **启动预热**：开屏播放期间预加载应用页面与静态资源，转场后不再出现空白页

## 🎨 界面优化
- GitHub 状态移至顶栏独立按钮（项目行更清爽）
- 打开文件夹入口移入 ⋯ 菜单；项目行保留快捷命令按钮
- 托盘 "omp" 字标笔画细化（更轻盈）
- 终端/命令菜单输入框支持方向键编辑

## 🔧 其他
- 更新历史可清空、更新弹窗可开关
- 网络代理 UI（自动检测 FlClash 等 / 手动 / 关闭）

## 🐛 修复
- **Markdown 链接吞字**：裸 URL 后紧跟的中文文字（如 `https://github.com/lizhipay/acg-faka，这个项目如何`）此前会被全部吞进链接地址；现正确还原为链接 + 相邻文字

**下载**：本页下方 Assets（macOS dmg / Windows exe / Linux AppImage）
